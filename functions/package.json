{
  "name": "exam-is-near-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Original Firebase Cloud Functions (Gen 2). Not deployed while on Spark — kept as-is so it can be redeployed unchanged once Blaze is enabled at 10k users. Blaze is required for onRequest/onCall/onSchedule v2 functions regardless of usage, so this cannot run on Spark at all.",
  "engines": { "node": "18" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.0",
    "razorpay": "^2.9.4",
    "node-fetch": "^2.7.0",
    "@google-cloud/bigquery": "^7.9.1"
  }
}
